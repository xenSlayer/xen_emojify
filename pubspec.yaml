name: xen_emojify
description: "Facebook like reaction widget, based on lottie flutter package."
version: 0.0.1
homepage: https://github.com/xenSlayer/xen_emojify

environment:
  sdk: ">=3.2.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  lottie: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
